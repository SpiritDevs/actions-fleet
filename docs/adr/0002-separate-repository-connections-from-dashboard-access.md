# Separate repository connections from dashboard access

The fleet can connect selected repositories owned by different people and organizations, but initially only Corey may access its dashboard through GitHub login. Repository authorization and dashboard membership are therefore separate relationships: adding a GitHub connection does not grant its owner or organization members access to the fleet. Future teammates receive explicitly allocated access instead of inheriting it from a connection.
